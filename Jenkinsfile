pipeline {
    agent any
    stages {
        stage('Debug') {
            steps {
                echo 'Hello from Jenkins'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
