pipeline {
    agent any

    stages {
        stage('Debug') {
            steps {
                echo 'Hello from Jenkins declarative pipeline'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
